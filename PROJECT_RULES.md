# Project Rules

## 项目定位

`TextGrabber` 是一个面向 macOS 的菜单栏截图 OCR 工具，强调：

- 轻量
- 原生体验
- 本地识别
- 低打扰交互

## 技术边界

- 平台：macOS
- 语言：Swift 6
- UI：AppKit + SwiftUI
- 打包方式：Swift Package Manager
- OCR：Apple `Vision`
- 截图：系统 `screencapture`

默认不引入第三方依赖，除非确有必要且收益明确。

## 实现原则

1. 优先保留系统原生行为与视觉语言。
2. 优先做最小可验证改动，避免无关重构。
3. 设置、快捷键、截图、OCR、结果展示之间的职责要保持清晰。
4. 涉及 UI 调整时，优先保证真实工具使用体验，其次才是预览便利性。
5. 涉及权限、快捷键和系统交互的改动，必须考虑失败路径与回退行为。

## 代码修改规则

1. 未经明确需求，不要扩大任务范围。
2. 不要为了“顺手优化”而改动无关模块。
3. 公共模型、服务层和 UI 层的边界应尽量稳定。
4. 如果引入新的状态、流程或文件结构，必须同步更新文档。

## 验证规则

在可行的情况下，优先执行：

- `swift build`
- `swift test`

如果改动涉及真实交互流程，额外关注：

- 快捷键是否还能正常触发
- 截图取消时是否安全退出
- 权限缺失时是否有正确提示
- 识别结果窗口是否可关闭、复制、重试

## 文档规则

以下情形应更新文档：

- 项目目标或范围变化
- 目录结构或模块职责变化
- 任务状态变化
- 会影响下一次会话接手的信息变化

最常更新的文档：

- `docs/ai-context.md`
- `docs/architecture.md`
- `docs/todo.md`
- `docs/dev-log.md`

## Git 规则

如需创建分支，优先使用短而清晰的任务名。

- 人工分支可采用：`feature/<task>`、`fix/<task>`
- 若由 Codex 代为创建分支，遵循当前工作环境要求，使用：`codex/<task>`
