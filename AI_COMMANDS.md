# AI Commands

## Purpose

该文件定义适用于本仓库的可复用 AI 指令模板。

当用户输入：`执行【命令名称】`
AI 应匹配本文件中的同名命令，并按对应模板执行。

## Invocation Format

- 标准格式：`执行【命令名称】`
- 可选参数：`执行【命令名称】（参数：...）`

## Execution Rules

1. 命令名称必须与“命令库”中的名称完全匹配。
2. 若命令不存在，AI 需提示“未找到命令”并列出可用命令。
3. 若命令存在，AI 按模板顺序执行，不跳步。
4. 执行后输出：
   - 已完成内容
   - 修改文件列表
   - 下一步建议（如有）

## Command Template Format

每个命令使用以下结构：

```md
### 命令：<命令名称>

触发词：执行【<命令名称>】
目标：<命令目标>
输入：<可选输入>
步骤：
1. ...
2. ...
3. ...
输出：
- ...
```

## Command Library

### 命令：上下文同步

触发词：执行【上下文同步】
目标：在新会话中快速恢复 `TextGrabber` 项目上下文。
输入：无
步骤：
1. 阅读 `AGENTS.md`、`PROJECT_RULES.md`。
2. 阅读 `docs/ai-context.md`、`docs/architecture.md`、`docs/todo.md`、`docs/dev-log.md`。
3. 扫描主要代码文件，重点关注 `AppCoordinator`、截图/OCR 服务、结果面板与设置窗口。
4. 输出当前能力、未完成事项、优先任务和潜在风险。
输出：
- 上下文摘要
- 风险与阻塞项
- 建议优先级清单

### 命令：开始开发

触发词：执行【开始开发】
目标：基于当前上下文启动一个开发任务。
输入：任务说明（可选）
步骤：
1. 从 `docs/todo.md` 选择最高优先级未完成任务，或使用用户指定任务。
2. 明确任务范围、涉及模块与不修改范围。
3. 实施最小可验证改动。
4. 运行必要验证。
5. 如状态发生变化，更新相关文档。
输出：
- 实施摘要
- 验证结果
- 修改文件列表

### 命令：文档刷新

触发词：执行【文档刷新】
目标：根据当前代码状态刷新协作文档。
输入：无
步骤：
1. 读取主要代码与配置文件。
2. 检查 `docs/ai-context.md`、`docs/architecture.md`、`docs/todo.md` 是否过期。
3. 更新过期内容并保持结构稳定。
4. 在 `docs/dev-log.md` 记录本次文档刷新。
输出：
- 文档变更摘要
- 修改文件列表

### 命令：代码审查

触发词：执行【代码审查】
目标：对当前改动进行风险导向审查。
输入：目标文件或范围（可选）
步骤：
1. 读取改动文件与关联逻辑。
2. 识别 bug、回归风险、边界条件与缺失测试。
3. 按严重级别输出发现项。
4. 给出最小修复建议。
输出：
- Findings 列表（含文件位置）
- 风险总结
- 修复建议

### 命令：发布前检查

触发词：执行【发布前检查】
目标：在提交或打包前完成基础质量检查。
输入：无
步骤：
1. 检查工作区状态与改动范围。
2. 执行可用的构建和测试命令。
3. 复核文档是否与当前实现一致。
4. 总结阻断项与发布风险。
输出：
- 检查结果
- 阻断项（如有）
- 可发布结论（是/否）

### 命令：结束会话

触发词：执行【结束会话】
目标：形成可交接的会话收尾记录。
输入：无
步骤：
1. 更新 `docs/ai-context.md` 的当前状态与下一步。
2. 更新 `docs/todo.md` 的任务状态。
3. 在 `docs/dev-log.md` 追加一条本次会话记录。
4. 汇总本次修改文件与后续建议。
输出：
- 会话总结
- 已更新文档
- 下一会话建议

### 命令：Git工作流

触发词：执行【Git工作流】
目标：按仓库约定执行可恢复的分支开发流程。
输入：任务名（可选）
步骤：
1. 阅读 `docs/git-workflow.md` 并确认当前阶段。
2. 推荐分支命名与最小命令序列。
3. 若涉及跨设备或收尾，提醒同步更新上下文文档。
4. 输出当前阶段的注意事项。
输出：
- 当前阶段命令清单
- 推荐分支名
- 需要更新的文档列表

## Maintenance

- 新增命令时，使用统一模板结构。
- 命令名称应短、明确、无歧义。
- 命令步骤应可执行、可验证、可交接。
